DEFINITION

1. System under test
    - sut
    - 테스트 될 대상
    - 한 컴포넌트, 모듈, 전체 코드가 될수있음 

2. Arrange, Act, Assert
    - clear, maintain하게 테스트 가능하게함
    - 모든 테스트는 다음 3단계에 따라서 작동됨
    - arrange : sut를 우한 pre condition 설정 
    - act : precondition 토대로 execute System
    - assert : expected로 실행된건지 verify

3. code coverage
    - 테스트를 실행했을 때 실제 소스 코드의 몇 %가 실행되었는지를 나타내는 지표
    - 측정 기준: 라인(Line), 분기(Branch - if/switch문), 구문(Statement), 클래스, 함수 커버리지 등
    - 주의사항: **"높은 커버리지가 높은 품질을 보장하지는 않는다"**는 점을 명심
    - 검증(Assertion) 없이 코드만 훑고 지나가는 테스트도 커버리지를 100%로 만들 수 있습니다. 이런 테스트는 아무런 가치가 없습니다.
    - 커버리지 수치 자체를 목표로 삼기보다는, 의미 있는 테스트를 작성했을 때 따라오는 부수적인 지표로 활용


4. Test doubles
    - sut를 dependencies(db, api, service)로부터 isolate 시키기 위함
    - 제한된 테스트 환경내에서 mimic real implementation (dependencies가 준비안된 상태거나 사용안될 떄에도 다른 코드를 테스트 위해서 사용)
    4.1 - Stub
            - context와 관계없이(어떤 input을 받던지) 항상 동일한 script내의 결과를 반환. 
            - 기상 api stub이면 테스트를 위해 항상 sunny만 반환
    4.2 - Spies
            - stub처럼 반환하지만 , 추가로 observer 역할
            - 메서드 얼마나 invoke됐는지, 어떤 argument받았는지, 호출 순서 등 기록함
    4.3 - Mocks 
            - Stub처럼 미리 정의된 응답을 제공하면서도, 동시에 기대되는 동작을 설정하고 이를 엄격하게 강제
            - strict validator
            - 어떤 인자 , call 횟수 등 전부 exactly defined되어있고 조금이라도 deviate되면 fail
            - 보안 프로토콜이나 API 사양 준수 여부와 같은 핵심적인 상호작용이 계약(Contract)을 따르는지 확인하는 데 유용
            - 

5. TDD
    - Test-driven development
    - 구현 코드를 작성하기 전에, 먼저 테스트를 작성하여 코드가 무엇을 해야 하는지 정의 -> 실제 요구사항에 집중할 수 있고 불필요한 코드의 유입을 방지 가능
    - RED-GREEN-REFACTOR
        - Red (실패하는 테스트 작성): 구현하고자 하는 동작을 설명하는 테스트를 먼저 작성합니다. 아직 코드가 없으므로 테스트는 실패하며, 이를 "Red" 단계라고 합니다.

        - Green (테스트 통과시키기): 테스트를 통과시킬 만큼의 최소한의 코드만 작성합니다. 이 단계의 목표는 완벽함이 아니라 기능 구현이며, 최대한 빨리 Red에서 Green으로 넘어가는 것이 핵심입니다.

        - Refactor (코드 개선): 테스트가 통과되면 코드를 정리합니다. 테스트가 계속 통과되는지 확인하면서 구조를 개선하고 중복을 제거하여 유지보수하기 좋은 코드로 만듭니다.

6. BDD
    - Behavior-driven development
    - 시스템의 동작을 기술적 지식이 있는 사람과 없는 사람 모두가 이해할 수 있는 방식으로 설명하고 검증하는 데 중점
    - 핵심은 테스트를 개발자, 제품 팀, 비즈니스 이해관계자 사이의 가교로 활용하여 요구사항에 대한 공통된 이해를 구축하고 소통을 개선
    - Gherkin 언어 사용 
        - Feature : ~ 
        - Scenerio : ~
        - Given : ~ 
        - When / And /Then
    - QA 등과 clear communication 원할 떄


7. CI
    - 지속적 통합 (Continuous Integration, CI)
    - 내가 작성한 코드가 다른 사람의 코드와 문제없이 잘 어우러지는지 '자주, 그리고 일찍' 확인
    - 자동화된 빌드와 테스트

8. CD
    - 1. 지속적 제공 (Continuous Delivery)
        - CI 파이프라인을 통과한 모든 변경 사항이 자동으로 빌드 및 테스트되어 스테이징(Staging) 또는 테스트 환경에 배포
        - 실제 운영 환경(Production)으로 출시할지 말지는 사람(개발자, QA 엔지니어 또는 릴리스 매니저)이 직접 결정 -> 최종 출시는 사람이 결정 
    - 2. 지속적 배포 (Continuous Deployment)
        - 변경 사항이 모든 자동화 테스트를 통과하면 사람의 개입 없이 즉시 운영 환경으로 배포
        - 운영 환경에 문제가 생기지 않도록 잡아낼 수 있는 회귀 테스트(Regression Test)를 포함한 강력한 자동화 테스트 세트가 필수 
        - 모든 변경 사항이 즉시 배포되기 때문에 수동 승인, 출시 전략 회의, 긴 배포 주기와 같은 병목 현상이 사라짐



9. Type of tests
    9.1 unit tests: for isolating, validating individual component
        - complete한 isolation에서 function, methods, classes 등 smallest testable units을 examine함
        - db, UI, API 등 broader context는 무시. 각 function에만 집중 
        - precision (validateCoupon()이 fail이면 coupon계산 로직 내에서 에러를 찾으면 됨 -> 디버깅 빨라짐)
        - speed (external dependencies 없기에 각 unit test는 빠른 시간 내에 종료됨)
        - unit 테스트는 잘 작동해도 다른 컴포넌트와 Integration시 에러 있을수도 있음 -> Unit 혼자 돌리진않음
    9.2 Integration test
        - components 간 잘 작동하는지 확인
        - overhead 준비필요 (실제 db, api 연동해서 확인함)
        - 각 테스트를 independent 하기 위해선, 테스트 전 known state 상태로 reset 해둬야함. (db cleaning, reinitializing external service)
        - unit test보다 slow
        - esssentail functionalities(checkout process, authentication system) 은 Integration, edge case validating은 unit test로 효율적이게 하자. 
    9.3 E2E Test
        - validate complete workflow. 
        - isolated parts만 테스트하는게 아닌, production 단계에서 모든 컴포넌트가 잘 작동하는지 확인
        - E2E는 black-box test(내부 설계를 모르는)와 유사 -> user-facing Behavior에 초점, code는 상대적으로 덜 신경씀
        - setup 복잡(db, api 등, 도커나 쿠버네티스로 편리하게 가능)
        - 작은 UI 변경에도 테스트 실패할수도있음
    
    이외의 다른 테스트들
        - performace testing(stress 상태에서 시스템 평가), usability test(user 경험 테스트), security test
        - 테스트는 check list가 아닌, spectrum임. 설계하는 시스템에 맞게, 적합한 테스트를 시행하자
    
10. Testing pyramid
    - unit, Integration, E2E 테스트를 fast, reliabel, maintainable하게 하기 위한 balance. 
    1. fast, hyper-focused한 unit test를 초기 단계에 가장 많이 실행
    2. well chosen인 E2E test 로 final safety 테스트. (가장 적게, 최소로 사용)
    -이 외에 실제 user experience를 위한 e2e를 가장 많이해라 혹은 Integration을 많이 해라 등 다양함. 



11. 실제 UNIT Test   
    11.1 equal function: 
        값 비교 후 같으면 pass -> 이후 라인이 실행됨
        다르면 마지막 인자인 콜백 메세지 출력 (어떤 err인지 state 하기위해 descriptive하게 작성하자)

        equal은 두 값이 같은 primitive values이자 same content여야만 equal이라고 함. 
        두 값이 객체라면 참조하는 메모리가 같아야만 같은 값으로 취급. 즉, 같은 구조에 같은 내용을 담아도 참조 주소가 다르면 다른 값이라고 판단
        객체의 구조, 내용만 같아도 같다고 출력하려면 deepEqual() 사용하자

        equal판단 여부 -> shallow comparison으로 === 연산자와 유사 ㅛㅗ

        일반 equal 은 shallow copy와 유사. 
        객체에 대해 shallow copy를 하면 객체의 최상위 수준의 속성만 복사 
        -> 속성값이 primitive면 값 그대로 복사되지만 객체면 메모리 참조만 복사됨 
        -> 내부 객체를 공유하게 되어 shallow copy 속성 변경 시 원본도 변경됨. 
    
    11.2 deepEqual 은 deep copy유사 
    객체 내부 모든 속성 및 중첩된 객체까지 재귀적 복사 
    -> 서로 다른 객체라 영향 안줌. 

    deepcopy 판단 알고리즘 : 
    1. objA === objB로 동일 참조인지 확인
    2. 각 obj 가 object 및 null 아닌지 확인 -> primitive 인지파악 
    3. Object.keys()로 각 객체 키 개수 파악 -> 다르면 객체 구조가다른것이기에 false
    4. 한 obj 요소로 다른 객체 순회하다가 객체 만나면 또 재귀적으로 1번부터 실행 


12. NODEJS test runner
    - Organizing test (한 파일에 관련된 code들 담아서 isolated 유지)
    - simple execution (watch mode로 테스트 recur 가능)
    - mocking too;(db, api) simulate 가능 
    - test file name convention -> filename.test.js
    - test파일 위치는 상관없음. 보통 해당 테스트 대상과 파일이 같은 디렉토리에 위치시킴
    - test 내부의 함수: function under test (FUT)
    - 한 파일에 여러개 test도 가능하다

    - test 함수에 전달되는 FUT 종류 
    테스트 함수를 정의하는 3가지 방식
        1. 동기 함수 (Synchronous Function)
        가장 기본적인 방식입니다. 함수가 실행되는 동안 에러(throw)가 발생하면 실패, 발생하지 않고 종료되면 성공으로 간주합니다.
        특징: 기다려야 할 비동기 작업이 없을 때 사용합니다.
        예시: test('테스트', t => { /* 로직 */ })

        2. 프로미스/async 함수 (Promise or async Function)
        비동기 작업을 테스트할 때 사용합니다. 프로미스가 거부(reject)되면 실패, 이행(fulfill)되면 성공으로 간주합니다.
        특징: async/await 문법을 사용할 수 있어 현대적인 자바스크립트 테스트에서 가장 권장되는 방식입니다.
        예시: test('테스트', async t => { await someAsyncWork(); })

        3. 콜백 방식 (Callback-based Function) — 상세 설명
        전통적인 비동기 처리 방식인 '콜백'을 지원하기 위한 기능입니다. 테스트 함수가 두 번째 인자로 **done**이라는 콜백 함수를 받습니다.

        A. 작동 원리
        테스트 러너는 테스트 함수에 done 인자가 정의되어 있으면, **"이 테스트는 done()이 호출될 때까지 끝난 게 아니다"**라고 판단하고 기다립니다.
        성공 케이스: done() 처럼 인자 없이 호출하거나 done(null), done(false) 등 Falsy한 값을 넘기면 성공입니다.\
        실패 케이스: done(new Error('fail')) 처럼 첫 번째 인자에 Truthy한 값(보통 에러 객체)을 넘기면 실패입니다.

        B. 주의사항: "혼용 금지" (가장 중요!)
        설명에 나와 있듯이, 콜백 방식(done)과 프로미스(async) 방식을 동시에 사용하면 안 됩니다.
        async (t, done) => { ... } 처럼 작성하면 테스트 러너는 혼란에 빠집니다. "프로미스 해결을 기다려야 하나? 아니면 done() 호출을 기다려야 하나?"를 알 수 없기 때문입니다.
        이 경우 테스트 러너는 친절하게도 "Passed a callback but also returned a Promise"라는 에러를 내뱉으며 테스트를 실패시킵니다.


    - subset: 
        - tests를 hierarchical structure로 구성가능
        - large test files에 대해서 related test cases grouping이 가능함! 
            -> improving readability and organization

        - NODE version 24 미만이면 -> async await 해줘야함


13. Watch Mode
    - node --test --watch로 실행
    - test suite는 한번만 run되고, process는 stay alive ->  monitoring test files + any imported module
    - 저장되면 re-execute됨
    - context switching없고, restart안해도 됨

+ 일반적인 node --test 실행 방식
1. 테스트 명령을 내린다.
2. 새로운 Node.js 프로세스를 띄운다.
3. V8 엔진을 초기화하고, 필요한 모듈(라이브러리 등)을 메모리에 로드한다.
4. 테스트를 실행하고 프로세스를 종료한다.
5. 여기서 발생하는 오버헤드는 엄밀히 말하면 OS의 CPU 컨텍스트 스위칭보다는 **'프로세스 기동 비용(Process Startup Overhead)'**에 가깝습니다

watch mode 시 context switching없고 효율적인 이유: 
    Node.js가 테스트 실행 환경을 메모리에 일정 부분 유지하려고 시도하기 때문입니다.
    일반 모드: 테스트를 한 번 실행할 때마다 프로세스를 완전히 새로 만듭니다. (Heavy함)
    Watch 모드: * Node.js 프로세스를 죽이지 않고 살려둔 상태에서 코드 변경을 감지합니다.
    변경이 일어나면 이미 로드된 환경(V8 인스턴스 등)을 재사용하거나, 최소한의 파일만 다시 읽어 실행합니다.
    즉, **"프로세스 생성 -> 모듈 로드 -> 엔진 초기화"**라는 무거운 과정을 매번 반복하지 않으므로 전환(Switching) 비용이 획기적으로 줄어듭


14. Executing subset of all tests
    - default test(별도의 인자없이 node --test 시): 
        1. .test.js (우리가 해오던 convention)
        2. -test.js OR _test.js
        3. /test directory내의 테스트 파일들
        4. test-*.js 형식의 파일들 
        전부 node --test 시 돌아감. ! 

    - targeted test execution with custom glob
        : node --test 05-suite-test/*.test.mjs 이렇게 작성 ! (책에선 ""로 감싸는데 내가 직접해보니 "없어야 작동함)
        - 특정 feature만 돌려볼수있음
        - custom convention 가능 (꼭 test.js 형식이 아니라 spec.js 하고 argument로 경로 명시해주면 됨)


    - 테스트 컨텍스트 활용한 test 방법들
        t.skip: 
            t.skip()은 이미 작성되었지만, 특정 이유로 잠시 실행하고 싶지 않은 테스트를 건너뛸 때 사용합니다. 
            예를 들어, 버그로 인해 실패하는 테스트나 아직 고쳐지지 않은 기능을 검사하는 테스트를 임시로 비활성화하는 데 유용

        t.todo: 
            t.todo()는 아직 작성되지 않았지만, 앞으로 작성해야 할 테스트를 표시할 때 사용
            "나중에 이 테스트 만들어야지"라고 표시해두는, 말 그대로 '할 일(To-do)' 목록 같은 개념
            ***일반 test와 마찬가지로 함수 내부 코드가 실제로 실행됨! 
            test fail 일 때 failing testㄹ고 콘솔에 뜸, 다만 실제 집계는 todo 1로 잡히고 pass/fail엔 표시안됨


        t.only:
            execlusive test용

15. test reporter
    - 1. node --test --test-reporter=spec  05-suite-test/suite.test.mjs : default 출력 방식
        ▶ Top level suite
        ✔ Subtest 1 (1.137409ms)
        ✔ Subtest 2 (1.40217ms)
        ✔ Top level suite (2.85488ms)
        ℹ tests 2
        ℹ suites 1
        ℹ pass 2
        ℹ fail 0
        ℹ cancelled 0
        ℹ skipped 0
        ℹ todo 0
        ℹ duration_ms 91.530788
    - 2. node --test --test-reporter=tap  05-suite-test/suite.test.mjs : tap(test anyting protocol)임. human/machine 둘다 readable -> 다양한 CI, 서드파티와 integrated 가능
        TAP version 13
        # Subtest: Top level suite
            # Subtest: Subtest 1
            ok 1 - Subtest 1
            ---
            duration_ms: 1.18128
            ...
            # Subtest: Subtest 2
            ok 2 - Subtest 2
            ---
            duration_ms: 1.49669
            ...
            1..2
        ok 1 - Top level suite
        ---
        duration_ms: 2.76623
        type: 'suite'
        ...
        1..1
        # tests 2
        # suites 1
        # pass 2
        # fail 0
        # cancelled 0
        # skipped 0
        # todo 0
        # duration_ms 91.978168
    - 3.  node --test --test-reporter=dot  05-suite-test/suite.test.mjs : 테스트 결과 점으로 나타냄(성공시 녹색, 실패하면 X) 
    ... 


16. assert 
    - assert 모듈은 어떤 값이 참일것이라고 단언하고 검증하는데 사용 
    - assert한 내ㅐ용이 거짓이면, AssertionError로 테스트 fail 시킴
    - assert.ok(value, [message]): value가 truthy인지 확인 (true, 1, 'hello', {}, [] 등 (false, 0, '', null, undefined, NaN이 아닌 모든 값))
    - assert.strictEqual(actual, expected, [message]): 두 값이 같은지 비교 (===), (객체는 메모리 참조 주소까지 같아야함)
    -  assert.deepstrictEqual(actual, expected, [message]): 두 값이 같은지 객체, 배열 내용을 재귀적으로 비교 

    - **이때 assert의 메세지는 test가 fail 일떄만, Assertionerr로 나옴!, pass면 안나옴. 


17. test의 종료 조건 
    1. 모든 assert 문 통과
    2. 다른 예외, err 발생안하는것 
    3. 제한 시간 내에 정상적으로 종료될것: async 함수가 마지막 줄까지 성공적으로 실행을 마쳐야 함


18. Mocking
    - 기존엔 task1Completed, Promise.withResolvers() 등 스파이 작업(몇갠지 counting하는 요소)을 수동으로 했어야 함 
    - NODE의 내장 기능인 모킹 기능 -> mock.fn()으로 각 작업을 감싸면, 스파이 생성됨. 테스트 종료 후에 mock.callCount()로 확인하면 됨 
    -> 불필요한 플래그 변수나 프로미스 로직이 사라짐 
    - Mocking은 외부 dependencies(filesystem, network, db)와 상호작용 시 powerful. 
      unit test 할 때 꼭 real I/O를 할 필요 없을 때 모킹하고 sut에만 fully focus. 
      

    -   test('mock.fn 예제', () => {
        // 아무 로직이 없는 가짜 함수 생성
        const myFn = mock.fn();
        myFn(1, 'a');
      
        // 가짜 함수에 실제 구현을 제공
        const sum = mock.fn((a, b) => a + b);
        sum(3, 4);
        console.log(sum.mock.calls[0].arguments) //[3,4] 출력
        console.log(sum.mock.calls[0].result) // 7 출력 
      });
    
    - test.mock.method(object, methodName, [implementation]) 
    이미 존재하는 객체의 특정 메서드를 가짜 구현으로 교체 
    fs.readFile, db.query, fetch 등등 특정 객체에 소속된 메서드 대체 (mock.fn은 독립적인 mock 함수 만드는 것)

    - 큰 장점은 테스트가 끝나면 자동으로 원래의 메서드로 복원시켜 준다는 점입니다. 이 덕분에 한 테스트의 모킹이 다른 테스트에 영향을 주는 "테스트 오염"을 방지할 수 있습니다.
    - BUT mock.fn의 경우 새로운 가짜 함수를 생성하는 것 (아무것도 대체하지않으며, 주로 콜백or DI 위해 사용 )
    
    - global 객체의 fetch 메서드를 아래의 async 함수로 대체함. 
    global = 전역객체. node에선 fetch는 전역객체임 
    (fs의 객체의 readFIle의 경우: t.mock.method(fs, 'readFileSync', () => fakeContent); 처럼 구현하면 됨)
    t.mock.method(global, 'fetch', async _url => ({
      ok: true,
      status: 200,
      headers: {
        get: key =>
          key === 'content-type' ? 'text/html; charset=utf-8' : null,
      },
      text: async () => mockHtml,
    }))


    - fetch 
        node built in fetch is using undici! 
        Pros:
            No additional dependencies required
            Works across different JavaScript runtimes
            Automatic compression handling (gzip, deflate, br)
            Built-in caching support (in development)
        Cons:
            Limited to the undici version bundled with your Node.js version
            Less control over connection pooling and advanced features
            Error handling follows Web API standards (errors wrapped in TypeError)
            Performance overhead due to Web Streams implementation
            */
            undici 따로 설치 안해도, console.log(process.versions.undici); //7.21.0 출력됨


    - Nodejs의 core module mock 
       t.mock.module (또는 t.mock.method)의 두 번째 인자는 **"해당 모듈을 import 했을 때 가져올 가짜(mock) 구현"**을 정의하는 부분입니다.

        access, mkdir, writeFile을 전부 목 함수로 만든 이유: saveConfig.mjs 파일이 내부적으로 fs/promises 모듈의 이 함수들을 사용하기 때문입니다. 단위 테스트(unit test)의 핵심은 테스트 대상(saveConfig)을 외부 환경(실제 파일 시스템)으로부터 "격리"하는 것입니다. 실제 파일 시스템에 접근하는 대신, 우리가 제공한 가짜 함수(mockAccess, mockMkdir 등)를 실행하게 만들어 saveConfig의 로직이 올바른지만을 검증하는 것이죠.
        테스트 시나리오 (정확합니다):

        첫 번째 (폴더 없음): mockAccess가 'ENOENT' 에러를 던져서 "폴더가 없다"는 상황을 흉내 냅니다. 그러면 saveConfig가 mkdir를 호출해야만 정상이므로, mockMkdir가 호출되었는지(assert.strictEqual(mockMkdir.mock.callCount(), 1))를 확인합니다.
        두 번째 (폴더 존재): mockAccess가 에러 없이 정상 완료되어 "폴더가 이미 있다"는 상황을 흉내 냅니다. 그러면 saveConfig는 mkdir를 호출하면 안 되므로, mockMkdir가 호출되지 않았는지(assert.strictEqual(mockMkdir.mock.callCount(), 0))를 확인합니다.

         또한, import 시 동적 import 사용, 
            mock이 된 후에!! import함. 
            만약 mock전에 import 한다면, saveConfig 내의 실제 모듈이 반영됨. 즉, t.mock.method 적용안됨
            mocking은 mock이 등록된 이후에 module이 로드되어야만 실헹됨
            t.mock.module('fs~', {
                cache: false, 
                namedExports: {
                    access: mock.fn(), 
                ~ 
                }
            })
            마찬가지로, cache: true면 기존에 가져온 모듈이 cache에 저장되서 mock 안됨
            default 방식이 아닌, {}를 통해서 namedExports할것이기에 mock 함수 정의 시 헤당 객체의 값으로 해줘야함. 

    - other dependency. 
    db, payment, logging 등을 테스트 할 경우, unit 테스트 시 isolated 하게 유지해야함,. 
    즉, 실제 invoke를 자제해야함, 
    똑같이, mock.module()로 정의해주면됨. 
    (import는 dynamic하게 ! )

    MODULE TEST의 단점: 
    - tight coupling(한 dependency가 변경되면 테스트 코드도 변경되어야 함 )
    - global scope (test 전역에서 영향 줌 -> afterEach()로 초기화 해서 해결함 )
    , complex setup, and constraints on test concurrency(현재 node ver에선 concurrency: false로 해야 모듈 테스트 가능, BUT 이건 performace를 낮추는 방향.. )

    이러한 단점들을 개선-> DI
    DI(chapter 7에서 배운 내용:  dependencies of a component are provided as inputs by an external entity, 
    often referred to as the injector. 
    This injector can be anything from a simple initialization script to a fully featured container that maps and wires dependencies across the application. 
    The main benefit of this approach is improved decoupling, especially for modules that rely on stateful or replaceable components (such as database clients or loggers). 
    Instead of creating or importing their dependencies internally, modules receive them from the outside, which makes them easier to reuse, configure, and test.)
    즉, DI 를 사용하면, 
    1. 동적 import 할 필요 없다
    2. 각 테스트를 concurrency하게 가능
    3. 각 테스트 종료 후 모듈 초기화 안해줘도 됨. (
        기존엔 mock.module로 mocking 하기위해서, dbMock 자체를 글로벌하게 둠
        그 결과  각 테스트 시 reset을 해줘야했음. 
        DI는 각 테스트 내부에 dbMock구현 -> 테스트 끝나몀ㄴ 영향 안주고 종료됨. 
        )




    

    19. Integration TEST 
    - unit 테스트로 db query 테스트 했었음. BUT unit테스트는 sql query가 올바른지, 실제 real 환경 리턴값과 가상 mock data가 같을거라고 확신 불가능 등의 우려가 있음. 
    -> INTEGRATION TEST

    -test에서 sqlite사용! 
        In-Memory 방식: filename: ':memory:' 설정을 사용하면 디스크에 파일을 쓰지 않고 메모리상에서만 DB가 돌아갑니다. 테스트가 끝나면 데이터가 자동으로 사라지므로 매우 깨끗하고 빠릅니다.
        Zero Setup: 별도의 DB 서버를 설치하거나 도커(Docker)를 띄울 필요 없이 라이브러리 설치만으로 즉시 실행 가능합니다.
        경량성: 프로세스 내부에서 직접 실행되므로 네트워크 지연이 없습니다.

    - integration테스트 시 차이점. 
    기존 unit 테스트에서는 query로 불러오는것과 balance 로직을 같이 했음. 
            /* canPay function에 인자로 db를 넣어서 dependency Injection 
        tight coupled된걸 해소가능. 
        dependency를 외부에서 가져와서 dependency가 수정된해도 테스트 로직은 변경 할 필요 없다. 
        */
        export async function canPayWithVouchers(db, userId, amount) {
            const vouchers = await db.query(
            `SELECT * FROM vouchers
                WHERE user_id = ? AND
                balance > 0 AND
                expiresAt > NOW()`,
            [userId]
            )
            const availableBalance = vouchers.reduce((acc, v) => acc + v.balance, 0)
            return availableBalance >= amount
        }
    BUT integration에선 sql query도 타당한지 확인해야함. 
    즉, query로직과 계산을 다른 함수로 구분! 

        export async function getActiveVouchers(db, userId){
        const vouchers = await db.query(
            `SELECT * FROM vouchers
            WHERE user_id = ? AND
            balance > 0 AND

            expiresAt > strftime('%FT%T:%fz', 'now')`, 
            [userId]
        )
        //strftime('%FT%T:%fz', 'now')는 SQLite 함수로, 현재 시간을 YYYY-MM-DDTHH:MM:SS:sssZ 형식의 문자열로 변환
        return vouchers
        }

        export async function canPayWithVouchers(db, userId, amount) { 
            const vouchers = await getActiveVouchers(db, userId)
            const availableBalance = vouchers.reduce((acc, v) => acc + v.balance, 0)
            return availableBalance >= amount
        }


    - 추가 상식.,
    promise를 리턴하는 db.query같은경우, 한 함수에서 로직이 프로미스 리턴이 전부라면 굳이 await db.query()가 아닌 return db.query로 프로미스 자체를 리턴해도 무방
    BUT 한 함수 내애서 다른 동기적 로직이 있다면 await 하자. ( addVoucher함수는 단순 프로미스를 반환하는게 아닌 record를 반환함. 
    즉, db query에 await 안해주면 record를 db에 저장안한채 동기적으로 반환. (db err이 난 경우에도 record는 정상적ㅇ로 리턴되므로 문제임. )  )


    20. Lazy initialization
    지연 초기화(Lazy Initialization) 패턴은 객체의 생성이나 값의 계산을 실제로 필요한 시점까지 미루는 디자인 패턴
    쉽게 말해, "일단 준비만 해두고, 누군가 '그거 줘!'라고 부를 때 비로소 만드는 방식"입니다.

    1. 왜 사용하는가? (장점)
    - 성능 최적화: 프로그램 시작 시 모든 자원을 미리 생성하면 구동 속도가 느려집니다. 지연 초기화를 쓰면 초기 로딩 시간을 단축할 수 있습니다.
    - 자원 절약: 생성 비용이 큰 객체(예: 데이터베이스 연결, 대용량 파일 로드)를 실제로 한 번도 사용하지 않는 경우, 불필요한 메모리 낭비를 막아줍니다.
    - 의존성 해결: 초기화 시점에 필요한 다른 리소스가 아직 준비되지 않았을 때, 나중에 연결을 시도함으로써 에러를 방지할 수 있습니다.

    2. 작동 원리
    2-1. 보통 내부적으로 null인지 확인하는 로직을 포함합니다.
    2-2. 객체 내부의 private 필드를 null로 둡니다.
    2-3. 해당 객체에 접근하는 메서드(Getter)를 호출합니다.
    2-5. 내부 필드가 null이면: 그때 객체를 생성하고 저장합니다.
        이미 존재하면: 생성 과정 없이 저장된 값을 반환합니다.

    3. 코드 예시 (Node.js/JavaScript)

    export class DbClient {
    #db; // 처음에는 아무것도 없음 (undefined/null)

    async #connect() {
        // 1. 이미 연결되어 있다면 새로 만들지 않고 기존 것을 반환 (재사용)
        if (this.#db) {
        return this.#db;
        }

        // 2. 실제로 필요한 이 시점에 비로소 비싼 자원(DB 연결)을 생성
        console.log("실제 DB 연결 중...");
        this.#db = await open({
        filename: ':memory:',
        driver: sqlite3.Database,
        });
        
        return this.#db;
    }

    async query(sql) {
        // 사용자가 쿼리를 날릴 때 비로소 connect가 호출됨
        const db = await this.#connect();
        return db.all(sql);
    }
    }

    4. 지연 초기화의 단점 
    - 첫 번째 호출의 지연: 누군가 처음 호출할 때 객체를 생성하므로, 그 첫 번째 사용자에게는 응답이 다소 느릴 수 있습니다.

    - 스레드 세이프(Thread-Safety): 멀티스레드 환경(Java, C# 등)에서는 여러 스레드가 동시에 if (null) 체크에 진입했다가 객체를 여러 개 생성하는 문제가 생길 수 있어 동기화 처리가 필요합니다. 
        (다행히 Node.js는 싱글 스레드 기반이라 이 문제에서 비교적 자유롭습니다.)

    5. 지연 초기화으 단점 해결 방안
    - 첫번째 호출이 느린걸 프리워밍으로 해결가능. 
    시스템에 여유가 있는 시점(예: 서버 부팅 직후 또는 특정 유저가 로그인한 직후)에 미리 한 번 호출해서 배경에서 초기화를 끝내두는 기법

    6. 효율적 사용법
    주로 테스트 환경에서 사용하면됨! 
    100개 중 10개만 할건데 eager로 하면 안쓰는 90개까지 모두됨

    <-------------> 
    lazy initialization 과 반대는 eager initialization.,
    즉, 코드 시작하면 바로 db든 뭐든 실행시킴. 
    에러를 즉각적으로 확인 가능해서 소규모 개발 시 유리
    export class EagerDbClient {
        constructor() {
        // 인스턴스를 만들자마자 바로 연결 시도
        this.db = this.connectSync(); 
  }
}


    21. sqlite 데이터 저장 방식 차이 
        1. in memory
        : integation test에서 각 테스트를 isolated 보장을 위해서 사용함, 
        filename으로 :memory: 전달. 
        -> 디스크에 저장이 아닌, RAM(메모리)에 저장. 
        - 프로세스 종료되면 사라짐. 
        - 자동화된 테스트를 위하여주로 사용됨. 
        
        2. 파일 기반 
        : const db = new DbClient('./my_database.sqlite') 일반적인 데이터베이스 형태
        - 데이터가 지정된 경로에 저장되서 파일에 그대로 남음
        - 속도는 디스크에서 가져오다보니 느림
        - 대부분의 애플리케이션에서 영구적으로 저장하기에 사용함. 

 
    22. E2E TEST
    - system이 implement된거보단, 어떻게 behave되는지를 외부에서 보는것. 
    - 사용자 관점에서 product validate, simulate user behavior .. 
    - integration test에서 진행한 테스트는 api만 테스트. 실제 user 관점으론 테스트 안해봄
    - e2e는 black box testing! 


    23.  Docker
    - image: 에플리케이션 실행을 위한 모든것(코드, 라이브러리, 설정) 등을 담아둔 읽기 전용 템플릿
    (ghcr.io/lmammino/sample-events-website:main 이게 이미지임) 
    - container: 이미지를 토대로 만든, 실제로 실행되고 있는 독립된 애플리케이션 공간 
    이미지의 살아있는 인스턴스다. 
    시작(docker run, docker start ID), 정지(docker stop ID), 삭제(ctrl C ) 가능하며 각 컨테이너는 서로 격리됨. (즉, 한 이미지로 여러개의 컨테이너 만들수잇다)
    docker run 통해서 이미지로 컨테이너 만듦. 
    
    - docker daemon: Docker의 총괄 매니저, 엔진(백그라운드에서 항상 실행됨) 

    -내가 직접 도커에 올리는 과정
    1. Dockerfile 작성: 내 서버를 어떻게 '이미지'로 만들지 설명하는 **레시피(Dockerfile)**를 작성합니다. (예: "Node.js 22 버전을 설치하고, 내 소스 코드를 복사한 뒤, npm install을 실행하고, 마지막으로 npm start 명령어로 서버를 실행해줘.")
    2. 이미지 빌드: 이 Dockerfile을 기반으로 docker build 명령어를 실행하여 나만의 '서버 이미지'를 만듭니다.
    3. 이미지 푸시(Push): 만들어진 이미지를 Docker Hub나 GHCR 같은 원격 레지스트리에 올립니다.
    4. 서버에서 실행: 실제 서비스가 운영될 클라우드 서버(AWS, GCP 등)에 접속해서, 방금 올린 이미지를 docker run 명령어로 실행합니다.

    - pull된 이미지는 하드 디스크에 영구적으로 저장됨. 컨테이너 종료/터미널 종료/ IDX 종료해도 계속 남아있음 

    - 이미지 삭제 
        docker images로 현재 이미지들 볼수있고, docker rmi(remove Image)

    - 컨테이너 조회(삭제,정지)
        docker ps -a (all)